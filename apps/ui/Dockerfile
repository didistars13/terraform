FROM nginx:alpine

# Copy your static files into the Nginx web root
COPY public/ /usr/share/nginx/html

# Optionally replace the default nginx config (optional)
# COPY nginx.conf /etc/nginx/nginx.conf

EXPOSE 80
